[project]
name = "maximus"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "pandas",
  "numpy",
  "scikit-learn",
  "scipy",
  "pyarrow",
  "xgboost",
  "catboost",
]

[tool.ruff]
line-length = 100

[tool.black]
line-length = 100
